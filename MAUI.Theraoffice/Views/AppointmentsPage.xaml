<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="MAUI.Theraoffice.Views.AppointmentsPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="Appointments">
    <VerticalStackLayout Padding="12" Spacing="8">
        <Button Text="Add Appointment" Clicked="OnAdd" />
        <CollectionView x:Name="List" SelectionMode="Single" SelectionChanged="OnSelect">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <VerticalStackLayout Padding="8" Spacing="4">
                        <Label Text="{Binding Start, StringFormat='{0:g}'}" FontAttributes="Bold" LineBreakMode="NoWrap"/>
                        <Label Text="{Binding Notes}" FontSize="12" TextColor="#666" LineBreakMode="WordWrap"/>
                    </VerticalStackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </VerticalStackLayout>
</ContentPage>
